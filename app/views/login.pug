extends layout

block content
	.container
		.row
			.col
				h1.mt-4 Login 
				form(action="/check", method="POST")
					.form-group
						input.form-control(type="text" name="username")
					.form-group
						input.form-control(type="text" name="password")
					.form-group
						button.btn.btn-primary(type="submit") Chơi
